{
  "_comment": "Escape hatch for per-project URL overrides. Normally not needed — the manifest builder reads each nested repo's 'homepage' field from GitHub (set it via repo settings → About → Website). Use this file only if GitHub's homepage field is wrong or inaccessible. Fields: liveUrl, description."
}
