#!/usr/bin/env node
/**
 * Export the tracker's JSON series as flat CSVs into data/downloads/.
 * Run after each data refresh (process-daily and ridership workflows call it);
 * safe to run by hand. Exits nonzero if a source file is missing or an output
 * would be empty — never silently publishes a hollow CSV.
 */
const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
const OUT = path.join(ROOT, 'data', 'downloads');
fs.mkdirSync(OUT, { recursive: true });

const readJson = (rel) => {
  const p = path.join(ROOT, rel);
  if (!fs.existsSync(p)) { console.error(`FATAL: missing ${rel}`); process.exit(1); }
  return JSON.parse(fs.readFileSync(p, 'utf8'));
};
const esc = (v) => {
  if (v === null || v === undefined) return '';
  const s = String(v);
  return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
};
const writeCsv = (name, header, rows) => {
  if (!rows.length) { console.error(`FATAL: ${name} would be empty`); process.exit(1); }
  const body = [header.join(',')].concat(rows.map((r) => r.map(esc).join(','))).join('\n');
  fs.writeFileSync(path.join(OUT, name), body + '\n');
  console.log(`${name}: ${rows.length} rows`);
};

// Bunching, gap, wait and "reliability" fields are deliberately NOT exported.
// A September 2026 audit found them indefensible (see index.html methodology);
// they remain in the JSON only until a replacement wait measure lands.

// 1. weekly system metrics
const weekly = readJson('data/summary/weekly.json');
writeCsv('weekly-system.csv',
  ['period', 'start_date', 'end_date', 'days', 'coverage_pct', 'comparable',
   'avg_speed_hour_norm_mph', 'avg_speed_raw_mph', 'avg_active_buses', 'peak_active_buses'],
  weekly.map((w) => [w.period, w.startDate, w.endDate, w.days, w.coveragePct, w.comparable,
    w.avgSpeedHourNorm, w.avgSpeed, w.avgActiveBuses, w.peakActiveBuses]));

// 2. weekly by borough — rebuilt from route rows so express routes (BM, BxM,
// QM, SIM, X) are their own group instead of being folded into a borough.
const weeklyRoutes = readJson('data/summary/weekly-routes.json');
const EXPRESS = /^(BM|BXM|QM|SIM|X)\d/i;
const groupOf = (route) => {
  if (EXPRESS.test(route)) return 'X';
  const r = route.toUpperCase();
  if (r.startsWith('BX')) return 'Bx';
  for (const c of ['B', 'Q', 'M', 'S']) if (r.startsWith(c)) return c;
  return null;
};
const boroAcc = {};
for (const [route, hist] of Object.entries(weeklyRoutes)) {
  const g = groupOf(route);
  if (!g) continue;
  for (const r of hist) {
    if (r.avgSpeed == null || r.daysSeen < 4) continue;
    const k = r.period + '|' + g;
    (boroAcc[k] ||= []).push(r.avgSpeed);
  }
}
const boroRows = Object.entries(boroAcc)
  .map(([k, v]) => { const [period, g] = k.split('|'); return [period, g, v.length, Math.round(10 * v.reduce((a, b) => a + b, 0) / v.length) / 10]; })
  .sort((x, y) => (x[0] + x[1]).localeCompare(y[0] + y[1]));
writeCsv('weekly-borough.csv', ['period', 'group', 'routes', 'avg_speed_mph'], boroRows);

// 3. weekly by route
const routeRows = [];
for (const [route, hist] of Object.entries(weeklyRoutes)) {
  for (const r of hist) {
    routeRows.push([route, r.period, groupOf(route), r.daysSeen, r.avgSpeed]);
  }
}
writeCsv('weekly-routes.csv',
  ['route', 'period', 'group', 'days_seen', 'avg_speed_mph'],
  routeRows);

// 4. route ridership monthly
const rm = readJson('data/ridership/routes-monthly.json');
const rmRows = [];
for (const [route, rec] of Object.entries(rm.routes)) {
  rm.months.forEach((m, i) => {
    if (rec.total[i] > 0) rmRows.push([route, m, rec.total[i], rec.wdAvg[i]]);
  });
}
writeCsv('route-ridership-monthly.csv',
  ['route', 'month', 'total_boardings', 'avg_weekday_boardings'], rmRows);

// 5. stop-level hourly boardings.
// Hour columns are MONTH TOTALS. Divide by weekday_days / weekend_days (also
// emitted per row, so the file is self-describing) for a per-day average.
const sp = readJson('data/ridership/stops.json');
const hourCols = [];
for (let h = 0; h < 24; h++) hourCols.push(`wd_total_h${h}`);
for (let h = 0; h < 24; h++) hourCols.push(`we_total_h${h}`);
writeCsv('stops-hourly-june2026.csv',
  ['stop_id', 'name', 'lon', 'lat', 'routes', 'month_boardings', 'month_alightings',
   'm_to_subway', 'in_crz', 'weekday_days', 'weekend_days'].concat(hourCols),
  sp.stops.map((s) => [s[0], s[1], s[2], s[3], (s[4] || []).join(' '), s[7], s[8],
    s[9] ?? '', s[10] ?? '', sp.days.wd, sp.days.we].concat(s[5], s[6])));

// 6. route geography classes
const rc = readJson('data/summary/route-classes.json');
writeCsv('route-classes.csv', ['route', 'bus_lane_share', 'cbd_relation'],
  Object.entries(rc.routes).map(([r, c]) => [r, c.busLaneShare, c.cbd]));

// 7. data dictionary — every column defined, generated alongside the data so
// it can never drift from it.
const dict = `NYC BUS TRACKER — DATA DICTIONARY
Generated by scripts/export_csv.js alongside the CSVs in this folder.
Project: https://github.com/joshgreenman1973/experiments/tree/main/nyc-bus-tracker

TWO SEPARATE DATA SOURCES LIVE HERE. Do not mix them.

  A. SPEED — our own observations from the MTA BusTime feed, collected in short
     bursts twice an hour since April 2026. These are estimates from GPS
     positions, NOT official MTA figures. On local routes they read about 9%
     faster than the MTA's published speeds, because readings under 0.5 mph
     (buses stopped at stops and lights) are dropped; routes rank in nearly the
     same order as the MTA's.

  B. RIDERSHIP (boardings) — the MTA's own Automatic Passenger Counter data,
     republished from data.ny.gov dataset fvdm-uavx. The MTA treats these as
     estimates: counters miss some riders and not every bus carries one.

  Bunching, gap, wait and "reliability" figures are no longer exported. An audit
  in September 2026 found them unreliable.

---------------------------------------------------------------- weekly-system.csv
One row per ISO week (source A).
  period                          ISO week, e.g. 2026-W30
  start_date, end_date            first and last date covered
  days                            days of data captured, out of 7
  coverage_pct                    share of operating clock-hours actually sampled
  comparable                      true when days=7 AND coverage_pct>=50
  avg_speed_hour_norm_mph         mph, every hour of day weighted equally <-- USE THIS to compare weeks
  avg_speed_raw_mph               snapshot-weighted mean; NOT comparable across weeks
  avg_active_buses, peak_active_buses   buses in the feed per snapshot (raw, not
                                  hour-normalized)

  Why "hour normalized": collection drops some runs, and which hours land varies.
  Bus speed swings ~3 mph across the day, so raw weekly means move with the
  sampling mix rather than with real service. Rows below 50% coverage or under
  7 days should not be compared head-to-head with full weeks.

--------------------------------------------------------------- weekly-borough.csv
One row per group per week (source A).
  group          M = Manhattan, B = Brooklyn, Bx = Bronx, Q = Queens,
                 S = Staten Island (local, limited and Select Bus Service routes,
                 assigned by the letters in the route name), X = all express
                 routes (BM, BxM, QM, SIM, X)
  routes         routes averaged that week (seen on at least 4 days)
  avg_speed_mph  plain mean of those routes' weekly speeds. NOT hour-normalized;
                 read as direction, not precision.

----------------------------------------------------------------- weekly-routes.csv
One row per route per week (source A). Same caveats as weekly-borough.csv.
  days_seen      days that week with any observation of the route

-------------------------------------------------------- route-ridership-monthly.csv
One row per route per COMPLETE calendar month (source B).
  total_boardings          all boardings that month, every day of the week
  avg_weekday_boardings    Monday-Friday boardings / calendar weekdays in the month.
                           Public holidays count as weekdays, so months containing
                           them read slightly low.
  Partial months are excluded entirely: the MTA series opens on 2024-09-30, and a
  single day divided by a full month's weekdays would read as a near-zero point.

------------------------------------------------------- stops-hourly-june2026.csv
One row per bus stop, June 2026 (source B). 13,351 stops.
  HOUR COLUMNS ARE MONTH TOTALS, NOT DAILY AVERAGES.
  wd_total_h0 .. wd_total_h23    boardings summed across all weekdays of the month
  we_total_h0 .. we_total_h23    boardings summed across all weekend days
  weekday_days, weekend_days     the divisors (22 and 8 for June 2026).
                                 avg weekday boardings in hour h = wd_total_h{h} / weekday_days
  hour                           MTA clock hour of the service date. Essentially all
                                 rows fall on their own calendar date; a handful
                                 (12 of 260,109 on a sample day) carry the following
                                 midnight and fold into hour 0.
  month_boardings/alightings     whole-month totals across all 30 days
  m_to_subway                    STRAIGHT-LINE meters to the nearest of the 496
                                 stations in the MTA Subway Stations file — not
                                 walking distance. That file includes the 21 Staten
                                 Island Railway stations, so the SIR counts as
                                 subway here. The map's "beyond the subway" layer
                                 uses 800 m: 38.3% of stops are beyond it counting
                                 the SIR, 41.5% if the SIR is excluded.
  in_crz                         1 if inside the MTA congestion relief zone —
                                 within the Central Business District polygon and
                                 NOT within one of the excluded roadway polygons
                                 (FDR Drive, West Side Highway/Route 9A, Battery
                                 Park Underpass, Hugh L. Carey Tunnel surface
                                 connections). The source geofence file does not
                                 label its polygons; the largest is taken as the
                                 CBD and the rest as the roadways the law excludes.
  Juneteenth (Friday, June 19, 2026) ran holiday service but counts as a weekday.
  298 stop IDs (0.63% of June boardings) had no GTFS location and are excluded.

------------------------------------------------------------------ route-classes.csv
One row per bus route.
  bus_lane_share   share of the route's shape LENGTH running within about 60 m of a
                   marked bus lane or busway (NYC DOT ycrg-ses3). Route and lane
                   geometry are both densified to 15 m before testing, so this is a
                   length share, not a share of raw vertices. Proximity does not
                   prove the route uses the lane — a lane on a cross street it
                   merely crosses can register.
  cbd_relation     "In CBD" / "Crossing CBD" from the MTA's own list (cgzt-smqf),
                   or "Outside CBD" when the MTA does not list the route.

  Comparing route groups: bus lanes get installed where traffic is worst, so a raw
  speed gap between lane and non-lane routes reflects where they run at least as
  much as what the lanes do. Compare each group against its own past.
`;
fs.writeFileSync(path.join(OUT, 'DATA-DICTIONARY.txt'), dict);
console.log('DATA-DICTIONARY.txt written');

console.log('CSV export complete.');
